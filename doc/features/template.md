# Feature User Story Template (Simple)

## 1) Basic Info

- **Feature ID**: `FEAT-XXX`
- **Title**: `<Short feature name>`
- **Status**: `<new | to-refine | refined | WIP | done | canceled>`

## 2) User Story

As a **<user>**, I want **<capability>** so that **<outcome>**.

## 3) Requirements

- `<functional requirement 1>`
- `<functional requirement 2>`
- `<validation/security/permission requirement if needed>`

## 4) Acceptance Criteria

1. Given `<context>`, when `<action>`, then `<expected result>`.
2. Given `<context>`, when `<action>`, then `<expected validation/error behavior>`.
3. Given `<edge case>`, when `<action>`, then `<expected result>`.

## 5) Implementation Notes

- `<implementation note 1>`
- `<implementation note 2>`
- `<dependencies, constraints, or assumptions>`

## 6) Tests

- **Tests to run**:
  - `<command>`
- **Coverage to add/update**:
  - `<unit/integration/e2e>`
- **Result summary**:
  - `<pass/fail + remediation>`

## 7) Done Checklist

- [ ] Requirements implemented.
- [ ] Acceptance criteria met.
- [ ] Tests added/updated and run.
- [ ] Docs updated if needed.
